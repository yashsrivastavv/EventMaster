{
    "rootDirectory": "client",
    "builds": [{ "src": "package.json", "use": "@vercel/static-build", "config": { "distDir": "dist" } }],
    "ignoreCommand": "echo 'Ignoring backend' && rm -rf server shared drizzle.config.ts"
  }